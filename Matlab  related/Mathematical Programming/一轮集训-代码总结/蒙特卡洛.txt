% 蒙特卡洛实际上是利用大量随机模拟的方式来估计结果
% 蒙特卡洛求圆周率代码如下：

N=1000000;    %随机点的数目
x=rand(N,1);  %rand 生成均匀分布的伪随机数。分布在（0~1）之间
y=rand(N,1);  %矩阵的维数为N×1
count=0;

for i=1:N
%如果点在圆内，计数器加1
	if (x(i) - 0.5)^2 + (y(i)-0.5)^2 <= 0.25
		count = count+1;
	end
end
PI=4*count/N